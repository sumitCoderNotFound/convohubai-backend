# Recruitment voice worker deps (run as a SEPARATE process, not in the API container)
# Install: pip install -r requirements-worker.txt
livekit-agents>=0.11.0
livekit-plugins-silero>=0.7.0
livekit-plugins-groq>=0.1.0
livekit-plugins-deepgram>=0.6.0
httpx>=0.26.0
python-dotenv>=1.0.0
